// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/koalatea/authserver/server/ent/publicjwkset"
)

// PublicJWKSetCreate is the builder for creating a PublicJWKSet entity.
type PublicJWKSetCreate struct {
	config
	mutation *PublicJWKSetMutation
	hooks    []Hook
}

// Mutation returns the PublicJWKSetMutation object of the builder.
func (pjsc *PublicJWKSetCreate) Mutation() *PublicJWKSetMutation {
	return pjsc.mutation
}

// Save creates the PublicJWKSet in the database.
func (pjsc *PublicJWKSetCreate) Save(ctx context.Context) (*PublicJWKSet, error) {
	var (
		err  error
		node *PublicJWKSet
	)
	if len(pjsc.hooks) == 0 {
		if err = pjsc.check(); err != nil {
			return nil, err
		}
		node, err = pjsc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PublicJWKSetMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = pjsc.check(); err != nil {
				return nil, err
			}
			pjsc.mutation = mutation
			if node, err = pjsc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(pjsc.hooks) - 1; i >= 0; i-- {
			if pjsc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = pjsc.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, pjsc.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*PublicJWKSet)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from PublicJWKSetMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (pjsc *PublicJWKSetCreate) SaveX(ctx context.Context) *PublicJWKSet {
	v, err := pjsc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pjsc *PublicJWKSetCreate) Exec(ctx context.Context) error {
	_, err := pjsc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pjsc *PublicJWKSetCreate) ExecX(ctx context.Context) {
	if err := pjsc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pjsc *PublicJWKSetCreate) check() error {
	return nil
}

func (pjsc *PublicJWKSetCreate) sqlSave(ctx context.Context) (*PublicJWKSet, error) {
	_node, _spec := pjsc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pjsc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (pjsc *PublicJWKSetCreate) createSpec() (*PublicJWKSet, *sqlgraph.CreateSpec) {
	var (
		_node = &PublicJWKSet{config: pjsc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: publicjwkset.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: publicjwkset.FieldID,
			},
		}
	)
	return _node, _spec
}

// PublicJWKSetCreateBulk is the builder for creating many PublicJWKSet entities in bulk.
type PublicJWKSetCreateBulk struct {
	config
	builders []*PublicJWKSetCreate
}

// Save creates the PublicJWKSet entities in the database.
func (pjscb *PublicJWKSetCreateBulk) Save(ctx context.Context) ([]*PublicJWKSet, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pjscb.builders))
	nodes := make([]*PublicJWKSet, len(pjscb.builders))
	mutators := make([]Mutator, len(pjscb.builders))
	for i := range pjscb.builders {
		func(i int, root context.Context) {
			builder := pjscb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PublicJWKSetMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pjscb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pjscb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pjscb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pjscb *PublicJWKSetCreateBulk) SaveX(ctx context.Context) []*PublicJWKSet {
	v, err := pjscb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pjscb *PublicJWKSetCreateBulk) Exec(ctx context.Context) error {
	_, err := pjscb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pjscb *PublicJWKSetCreateBulk) ExecX(ctx context.Context) {
	if err := pjscb.Exec(ctx); err != nil {
		panic(err)
	}
}
