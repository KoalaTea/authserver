# reason
Zymkey4 is supposed to come with 3 slots. I want to see if there are 3 slots and if so that they are different and all provide working keys. This should probably be an actual test in Zymkey package to also partially act as documentation but I am going to ignore that for now.
# result
